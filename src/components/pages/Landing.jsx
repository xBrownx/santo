import { memo } from 'react';
import { landing as CONST } from '../../constants'



const Landing = memo(
    function Landing() {
        return (
            <>
            </>
        );
    }
);

export default Landing;