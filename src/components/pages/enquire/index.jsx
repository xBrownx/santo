import { forwardRef, memo } from 'react';
import { Page } from '../../templates';


const Enquire = memo(
    forwardRef(
        function () {
            return (
                <Page>

                </Page>
            );
        }
    )
);

export default Enquire;